# What /var/www/html is for

This usually is for your-software-specific nginx-related html files.

Make a new folder `{{ your-software }}` here and then put inside a `public` subfolder

`/var/www/html/{{ your-software }}/public`